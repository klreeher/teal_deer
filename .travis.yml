language: python
python:
  - '3.5'
# command to install dependencies
install: "make"
# command to run tests
script:
  - |
    make init
    make html
#    make github
  
deploy:
  provider: releases
  api_key: $GITHUB_ACCESS
  file_glob: true
  file: output/*
  skip_cleanup: true
  draft: true